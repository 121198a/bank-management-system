# The checked-in frontend lockfile currently contains a Vite 8 tree with an older
# React plugin peer range. Keep installs deterministic until the lockfile is
# regenerated with the Vite 8-compatible @vitejs/plugin-react release.
legacy-peer-deps=true
